## Rust HTTP Server

A lightweight HTTP server built from scratch in Rust.

The project focuses on understanding how an HTTP server works internally, including TCP connections, HTTP request parsing, response handling, concurrent request processing, and thread pools.
